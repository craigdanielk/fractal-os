/****
 * Manufacturing Module Index
 *
 * Provides deterministic access to manufacturing patterns and
 * operational models. This module is intentionally minimal —
 * it exists to demonstrate how FractalOS can extend beyond
 * digital workflows into physical operational systems.
 */

import patterns from "./patterns.json";

export const manufacturingModule = {
  version: "1.0.0",
  patterns
};
