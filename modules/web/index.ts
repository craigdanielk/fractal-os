/****
 * Web Module Index
 *
 * Provides access to reusable web build patterns
 * (sections, layouts, component recipes, scaffolds).
 *
 * This module does NOT create websites autonomously.
 * It only exposes pattern metadata for human-triggered
 * scaffolding workflows in FractalOS.
 */

import patterns from "./patterns.json";

export const webModule = {
  version: "1.0.0",
  patterns
};
